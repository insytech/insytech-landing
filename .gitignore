# build output
dist/

# generated types
.astro/
.vercel/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.production

# editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# macOS-specific files
.DS_Store

# local agent / tooling config
.claude/
CLAUDE.md
Makefile
.vanskills.lock
# Índice de skills que la herramienta regenera sola: depende de qué haya
# instalado en cada máquina, así que en el repo solo produce conflictos.
.atl/
# Config del servidor MCP de Search Console: apunta a una credencial que vive
# fuera del repo, así que cada quien la suya.
.mcp.json
# Planes de trabajo locales (no se commitean)
plans/
.impeccable/

# Working files for design canvases (the canvas itself lives in the Artifact)
.design/

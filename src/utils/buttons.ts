// Extra classes that turn BtnLink's `secondary` variant into the outlined
// white button of the home hero ("Ver Servicios"). Shared so every page that
// wants that button reuses it instead of re-deriving the look.
export const OUTLINED_BUTTON =
    "py-4 px-10 font-semibold border-2 border-gray-200 dark:border-gray-800 hover:bg-gray-50 dark:hover:bg-white/5 transition-all rounded-full";

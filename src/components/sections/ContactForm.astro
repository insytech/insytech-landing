---
import Container from "../shared/Container.astro";
import Title from "../shared/Title.astro";
import Paragraph from "../shared/Paragraph.astro";
import Button from "../shared/Button.astro";
---

<section id="contact" class="py-20">
    <Container className="space-y-10">
        <div class="text-center max-w-3xl mx-auto space-y-4">
            <Title> ¿Tienes un proyecto en mente? </Title>
            <Paragraph>
                Conversemos sobre cómo aplicar automatización, trazabilidad o
                visión computacional en tu operación.
            </Paragraph>
        </div>
        <div
            class="max-w-3xl mx-auto bg-box-bg border border-box-border shadow-lg shadow-box-shadow rounded-2xl p-6 sm:p-8"
        >
            <form action="#" class="space-y-6">
                <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                    <div>
                        <label
                            for="name"
                            class="block text-heading-3 font-medium mb-2"
                            >Nombre y Apellido*</label
                        >
                        <input
                            type="text"
                            id="name"
                            name="name"
                            required
                            class="w-full px-4 py-3 border border-box-border rounded-lg bg-gray-50 focus:outline-none focus:ring-2 focus:ring-primary"
                        />
                    </div>
                    <div>
                        <label
                            for="company"
                            class="block text-heading-3 font-medium mb-2"
                            >Compañía*</label
                        >
                        <input
                            type="text"
                            id="company"
                            name="company"
                            required
                            class="w-full px-4 py-3 border border-box-border rounded-lg bg-gray-50 focus:outline-none focus:ring-2 focus:ring-primary"
                        />
                    </div>
                </div>
                <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                    <div>
                        <label
                            for="email"
                            class="block text-heading-3 font-medium mb-2"
                            >Email*</label
                        >
                        <input
                            type="email"
                            id="email"
                            name="email"
                            required
                            class="w-full px-4 py-3 border border-box-border rounded-lg bg-gray-50 focus:outline-none focus:ring-2 focus:ring-primary"
                        />
                    </div>
                    <div>
                        <label
                            for="phone"
                            class="block text-heading-3 font-medium mb-2"
                            >Teléfono*</label
                        >
                        <input
                            type="tel"
                            id="phone"
                            name="phone"
                            required
                            class="w-full px-4 py-3 border border-box-border rounded-lg bg-gray-50 focus:outline-none focus:ring-2 focus:ring-primary"
                        />
                    </div>
                </div>
                <div>
                    <label
                        for="message"
                        class="block text-heading-3 font-medium mb-2"
                        >Describe tu operación, desafío técnico o lo que estás
                        buscando.</label
                    >
                    <textarea
                        id="message"
                        name="message"
                        rows="4"
                        class="w-full px-4 py-3 border border-box-border rounded-lg bg-gray-50 focus:outline-none focus:ring-2 focus:ring-primary"
                    ></textarea>
                </div>
                <div class="text-center">
                    <Button variant="primary" className="w-full sm:w-auto">
                        Solicitar diagnóstico técnico
                    </Button>
                </div>
            </form>
            <Paragraph className="text-center text-sm mt-4">
                Al enviar este formulario, aceptas nuestro <a
                    href="#"
                    class="text-primary font-medium underline"
                    >aviso de privacidad</a
                >.
            </Paragraph>
        </div>
    </Container>
</section>

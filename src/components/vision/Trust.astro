---
import SocialProof from "../shared/SocialProof.astro";
import { SURFACE } from "@utils/typography";

// El bloque vive en shared/SocialProof.astro desde que /software/ lo necesitó.
// Aquí solo queda la superficie de /vision/.
//
// Va inmediatamente después del Hero a propósito: la prueba sirve antes de
// pedirle al visitante que lea seis capacidades.
---

<SocialProof class={SURFACE.base} />

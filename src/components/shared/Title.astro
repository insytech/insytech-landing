---
export interface Props {
    className?: string;
    as?: "h1" | "h2" | "h3" | "h4";
}

const { className = "", as: Tag = "h1" } = Astro.props;
---

<Tag
    class={`text-heading-1 font-semibold text-2xl sm:text-3xl md:text-4xl ${className}`}
>
    <slot />
</Tag>
